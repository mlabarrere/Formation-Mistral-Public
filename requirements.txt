# Environnement Python de la formation Mistral CD39.
#
#   python3.12 -m venv .venv && .venv/bin/pip install -r requirements.txt
#
# Python 3.12 recommandé. Les lignes ci-dessous sont pré-1.0 et n'ont pas été
# validées sur 3.14.
#
# Versions figées d'après l'environnement qui a exécuté les quatre notebooks
# du Jour 1 de bout en bout, sans erreur.

# ── Contrainte structurante ───────────────────────────────────────────────────
# langchain-core DOIT rester en 0.3.x : les lignes 1.x exigent
# langchain-core>=1.4.7, qui casse ragas (utilisé par T1a et T4).
# C'est cette contrainte qui fige langgraph et langchain-mistralai en pré-1.0.
langchain-core==0.3.86
langgraph==1.0.10rc1
langchain-mistralai==0.2.12

# ── Mistral ───────────────────────────────────────────────────────────────────
mistralai==2.9.4
mistralai-workflows==3.11.0        # SDK en preview

# ── Évaluation (T1a, T4) ──────────────────────────────────────────────────────
ragas==0.4.3
litellm==1.98.0
instructor==1.16.0

# ── Domaine et visualisation ──────────────────────────────────────────────────
gpxpy==1.6.2                       # trace GPS du fil rouge GR509 (T1)
networkx==3.6.1                    # topologie de l'usine (T2, usine_viz.py)
matplotlib==3.11.1
pandas==3.0.5
requests==2.34.2
pydantic==2.13.5
python-dotenv==1.2.3

# ── Exécution des notebooks ───────────────────────────────────────────────────
ipykernel==7.3.0
nbclient==0.11.0
nbformat==5.11.1
